name: Deploy to VM

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Deploy
        run: |
          cd ~/15_Soeun_Sovannarith_PVH_Web_Homework005/my_app
          git pull origin main
          docker compose up -d --build
          docker image prune -f